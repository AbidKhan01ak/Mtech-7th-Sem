
          foreign [Music] so in this session before we go on to network embedded applications we'll try to understand what's the difference between a multi-processing system and a distributed system now in case of a multi-processing system also we are going to have multiple embedded devices in case of a distributed system as well we are going to be having multiple embedded devices so what exactly is the difference between calling a system as a multi-processing system and calling it as a distributed system
          
          so when we're talking about any system a system has three components as we know it need not be an embedded system any kind of system has three components it has Hardware it's going to be having control and it's going to be having data so the hardware in case of an embedded system would be an embedded processor you would be having an application which is what it's going to be controlling and there is going to be data which is associated with the application this is not only true of embedded systems so any
          
          kind of system would have Hardware control and data now before we understand what is going to be a distributed system there are number of definitions available or there are different people who are come up with different kinds of definitions for distributed system one way to describe a distributed system is to talk about the attributes of a distributed system so when we say it's a distributed system we say that it has an arbitrary number of systems and application processes so there is nothing like a predefined
          
          number of system and application process so we say there are large number of logical resources and these cannot be usually defined by a standard number whatever is going to be the physical architecture would be modular in nature so when even when we're designing the system we go in for a modular design so when we given an application we would start developing a certain module of it and then we go on to build the rest of the application in any kind of distribution there is going to be some kind of communication that is going to
          
          be happening communication could be done using message passing via shared communication systems this communication system could be wired or it could be Wireless but even though the system is going to be distributed the control is going to be system wide because the distributed system is designed for a particular application so these are some of the attributes or characteristics which are expected of a distributed system so this is one way of defining a distributed system uh there is actually a definition which comes
          
          from shoulder shoulder gives a list of symptoms of how you can describe a distributed systems so according to him a distributed system must have multiple processing Elements which is true when we are talking about any kind of network embedded system a network embedded system is definitely a distributed system in a network embedded system you are going to have multiple processing elements it's not going to be one embedded device you could have multiple embedded devices and all these embedded devices are part of the application and
          
          hence they are going to be interconnected with each other so again let's take a vehicle as an example as I said a vehicle would have 100 plus embedded nodes in it or would have 100 plus embedded microcontrollers in it so it has multiple processing elements and all these embedded controllers would be connected by some form of bus it could be flux Ray it could be can it could be ttpc but they're going to be connected by some form of bus so you're going to have interconnected hardware and here the main characteristic of a distributed
          
          system is if processing element is going to fail it is only that particular processing element that will fail the rest of the system would not fail with it so every processing element if it fails it fails independently it doesn't bring down the system with it just because the embedded controller which is controlling the AC in your car fails doesn't mean that your car should stop running so your vehicle system will still continue to function the only thing is the AC control the temperature control of your car will no longer be
          
          happening so the main characteristic of a distributed system is that there will be error containment or fault containment so if one part of the system is going to fail or one processing element is going to fail it doesn't mean the rest of the systems would fail Accord along with it of course there will be sharing States so when we are starting a network embedded application or a distributed system all of them would be in the initialization state or they will go through the initialization routine from the initialization routine
          
          they will go through a normal routine so they would share States but then there are going to be multiple processing elements they are going to be connected with each other the main characteristic of this is going to be if a processing element fails it fails independently it does not bring down the system to it distributed systems to a large extent have been suggested as alternate solutions to having a very single powerful microcontroller or a microprocessor the main advantage of having a distributed system is Even If
          
          part of your system fails your application will still continue to run so that's one of the major advantages of a distributed system and it also becomes a major symptom of the distributed system that if a processing element fails the application should not fail with it so there is one form of definition another is given by enslow enslow says that if it's a distributed system the hardware should be distributed the control should be distributed the data should also be distributed so as I said any system is
          
          going to have three parts to it that's going to be Hardware it's going to be control and there is going to be data if the system is distributed then the hardware should be distributed the control should be distributed and the data should be distributed now let's try to understand what exactly is going to be distributed Hardware or what do we mean by distributed control or what exactly is the significance of distributed data when we are going to talk about Hardware in any kind of embedded system it's
          
          possible that you have only a single Central Processing Unit you would have a control unit which is there with the CPU you will have a single ALU maybe you're going to build your system around let's say an 8051 microcontroller or you're going to build it around one of the simpler 8 16 bit or 32-bit microcontrollers that are going to be available a single core system so in such cases we are going to have a single CPU a single control unit and a single ALU it's also possible that when you're building an embedded system you could
          
          build it around a system which is going to be super scale R in HR so when we say it's a scalar system we are going to say that in a scalar system only one instruction enters at a time there will be only one control unit and essentially there will be only one arithmetic and logic unit another way to do it is you can have a super scalar processor in a super scalar processor you would have a single control unit but there would be multiple Al use or multiple arithmetic and logic units that are going to be present so
          
          more than one instruction can come in simultaneously each instruction would go to a different ALU but the release of the instruction and the control of the instruction or the movement of the instruction through the central processing unit would be done by a single control unit or you could have a multi-core system in case of a multi-core system you could have multiple functional units or you could have multiple CPUs one could be a standard CPU another could be a floating point a CPU or you could have a third
          
          CPU which is going to be used only for Io processing or you could have a homogeneous multi-core system for example if you're going to have your i7 your I5 processors or most of the newer arm cortex processors all of them are going to be multi-core systems they have multiple cores the course can be homogeneous or they could be heterogeneous schools but at the end of the day there is going to be a single piece of hardware and this piece of Hardware is going to be having multiple arithmetic or logic units could have
          
          multiple control points when we are going to be talking about a system which is going to have multiple processing elements it means that you would have multiple CPUs which would be separated out from each other they might be sharing an i o system or they could be sharing a global memory or you could have multiple CPUs or multiple Computing units they have their own local iOS and local memory so the possibility is you have multiple processing Elements which are going to be sharing their IO and memory system or
          
          you could have multiple CPUs which are going to be having their own local i o and local memory systems so generally if your Hardware is going to fall into these two categories your Hardware becomes distributed but please remember here it is not sufficient that your Hardware is distributed enslow says that the hardware should be distributed the control should be distributed the data should also be distributed now what exactly do we mean by distributed control when we're talking about an application you could have a single
          
          fixed point of control for that application the system may have a single CPU may have multiple CPUs CPUs might have their local iOS they might have a global IBO or Global memory but they have a single point of control now this single point of control would be fixed so I would be having a system which is going to be the major CPU the rest of them are going to be connected to the the major CPU or to the master CPU you could have a single Dynamic control Point meaning that one of the CPUs is currently responsible for running the
          
          control algorithms it's then moved to the next CPU and so on what we call as a fixed Master Slave structure versus a dynamic Master Slave structure so you could have a system where you have multiple CPUs one CPU was going to be the master the others are going to be the Sleep the master may be fixed the master may be dynamic so such a system would not be a distributed control system so what do we mean by a distributed control system when we say there is a distributed control system if I have different embedded devices or
          
          there are going to be different processing elements each of these processing elements have the same control point or they're going to have different control points each embedded device is going to be its own master or there is going to be a control algorithm which is going to be running on each of these embedded devices the embedded devices might be of the same type or they might be of different types so I could be running different control algorithms on different embedded devices so when we're talking about multiple
          
          homogeneous control points or multiple heterogeneous control points there is nothing like a Master Slave system every a node or every processing element in the system is going to have equal priority in the system so there will be control algorithms running on each of these embedded devices or each of these CPUs they could be having the same control algorithm running they might have different control algorithms running but then there are going to be there is nothing like a master and the rest of them are
          
          going to be sleeves so the master is going to be the controlling entity the master says this is the time at which you have to collect data the data is going to be collected if the master says switch on a particular thing then unit the unit gets turned on and so on so every embedded device here is capable of running its own control algorithm the control algorithm running on these might be having similar characteristics or there might be different kinds of control algorithms that are going to be running so even if you have multiple
          
          CPUs if you have a single point of control then it is considered to be a centralized control system if you have different points of control or you have multiple points of control then it becomes a distributed control system so for a distributed system the control system must also be distributed in nature the third parameter that is the data also has to be distributed in nature so for instance if I have a database I could have a centralized database with a single copy of the files and directory so any processing element
          
          if it needs data has to access this database and get the data from the database what could be the other option is I would have a centralized directory and the files could be available with the different processing elements but each processing element will not have its local directory so if I want data I will still go to the centralized directory the centralized directory would then get the data for me from the local directories and give from the local systems and give it to me the other possibility is I can have a replicated
          
          database or whatever is a database is going to be copied on different processing elements so if there are 10 processing elements there will be 10 copies of the database and each of these would have its own copies of files or directory so issue here would be anytime you update the data base in one place you have to ensure that all the nine copies are also going to be updated so that's a replicated database the other is where you have the database partition between different processing elements and there is going to be someone who is
          
          the master and the master will have a duplicate copies of all the files that are present but still remember there is a master so I would contact the master or I could contact any one of the other nodes for the data but then there is still a master database so such a kind of system still points towards a centralized Master of the database the when it says the distributed system it essentially means that I need to have a partition database with the master that is only going to keep a copy of the complete directory so if I want to
          
          access data I might contact the master directory the master directory might say that the data is going to be present in X node or Y node and I get the data from the X node or the Y node the other option is I have a database the database is going to be partitioned and nobody has a master file or Master directory so what would happen is if I need data I have to send out a query someone would respond to a it's a query so if it is a partition database with the master directory my query will always go to the
          
          master directory if it is going to be a partition database with no master directory the query is usually broadcasted and someone will be replying towards the query each of these are considered to be distributed data now what n slaus does this it says that in case of a system you could have different types of control which is going to start from a single point of control to multiple heterogeneous points you could have a single CPU with a single ALU you can have multiple CPUs which are going to be connected to each
          
          other or multiple heterogeneous CPUs the data could be a single centralized database to a partition database with no one having the single copy of the file or the directory we say a system is fully distributed only if it has heterogeneous CPUs heterogeneous control points with database completely partitioned and no one going to be having a central directory but what is accepted as allowed area for DS is we could have either homogeneous CPUs or heterogeneous CPUs we could have heterogeneous control points or
          
          homogeneous control points we could have a database that is partitioned if there may or may not be a master directory so all of this is said to be an allowed area for distributed system so this is one possible way of defining a distributed systems using n slos model now those of you who are familiar with multiprocessor architecture would know that there is something which is called A Shard memory architecture where you would have multiple processors which are going to be connected towards a common
          
          shared memory what is called a Shad memory architecture the other which is called as uniform memory architecture where you are going to be having different memory processors having their own memory devices the processors are going to be connected towards the interconnection network so every processor has a local memory and there is a non-uniform memory architecture version of it where all processors will have their own memory other than that there would be some amount of shared memory so for instance when we are going
          
          to talk about multi-core processors such as I5 or i7 there is going to be multiple CPU cores each core has its local cache it has a local level one cache level 2 cache and it has a shared level 3 caching so you could have some amount of local memory you could have some amount of shared memory so that is called as a non-uniform memory architecture uniform memory architecture a non-uniform memory architecture it comes in the falls under the category of distributed memory architectures but all of these please understand are going to
          
          remind us multiprocessing systems so when we are going to say it is a multi-processing system we could have a simple single embedded system which is made up of more than one processor now this processor could be using shared memory architecture it could be using distributed memory architecture in case of distributed memory architecture it could be uniform memory architecture it could be non-uniform Memory architecture but there are multiple processors which are going to be connected to each other
          
          when we are talking about a distributed system or we are talking about a network system we are going to have multiple such embedded nodes each of these embedded nodes could have more than one processor in it it could have one processor or it could have more than one processor each of these embedded nodes are going to be explicitly connected using some form of bus so if for example in a vehicular Network you might have multiple such embedded nodes available these nodes would be connected via a can bus or a flex array
          
          bus or a TTP bus even in case of Industrial Systems you could have multiple such embedded nodes each of these embedded nodes might be uniprocessing systems may be multi-processing systems who are going to be connected via buses or instead of buses they could be connected via networks so when we are talking about a network embedded system we are essentially talking about a distributed system a distributed system could be made up of multiple nodes each of these nodes in themselves could be a single
          
          Processing Unit processing systems or multi-processing systems so interconnected network of embedded devices or interconnected bus of embedded devices is what falls under the category of distributed systems or false under the category of network embedded systems so this is where the difference comes between distributed systems and multi-processing systems [Music]
          